config BR2_PACKAGE_THUNDER_SPOTIFY
	bool "Spotify"
	select BR2_PACKAGE_ALSA_LIB
	help
	   WPE Platform plugins that supports spotify services.
            - Spotify connect

if BR2_PACKAGE_THUNDER_SPOTIFY
# options for the plugin
endif
